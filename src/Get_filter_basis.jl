module Get_filter_basis

    """
        Returns a filter basis array with filters of size 'filter_size'.
        - filter_size can be an array.
    """
    function get_filter_basis(filter_size)
        print(filter_size)
    end

end